import React from "react";

export default function ConnectSection() {
  return (
    <section id="connect" className="py-20 bg-white">
      <div className="max-w-7xl mx-auto px-6">
        <h2 className="text-3xl font-semibold mb-4">Connect</h2>
        <p className="text-neutral-600">Gateway hub: Get Started, Sign Up, Enterprise, Support, Community. Entry and onboarding entry points (UI only).</p>
      </div>
    </section>
  );
}
